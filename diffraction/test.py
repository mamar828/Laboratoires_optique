# from scipy.optimize import curve_fit

# def func(x, a, b):
#    return a*x*x + b

# for b in range(10):
#    popt, pcov = curve_fit(lambda x, a: func(x, a, b), 0, 10)


print(list(zip(range(2), [1,2,3,4], ["a","b","c","d"])))